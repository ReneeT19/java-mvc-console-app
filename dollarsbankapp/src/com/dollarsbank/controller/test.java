package com.dollarsbank.controller;

import java.lang.reflect.Array;

public class test {
    private static Object Array;

    public static void numberOfWays(Integer number) {
        Array = n(number+1);
        n(0) = 1;
        for(int i = 0; i) IN [1..N] {

        }
        number[k] = 0
        FOR d IN [1..min(5, k)+1]
        n[k] += n[k - d]

        RETURN n[N]
    }

}
